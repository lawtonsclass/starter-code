#include "bitOps.h"

// An int has bit 0 through bit 31
bool isBitSet(unsigned int n, int i) {
  // FIXME: return true if bit i of the number n is set
  return true;
}

int countBits(unsigned int n) {
  // FIXME: return the number of bits that are set to 1
  // you'll probably want to use the isBitSet function!
  return -42;
}

void setBit(unsigned int& n, int i) {
  // FIXME: set the ith bit of the number n to 1
}
