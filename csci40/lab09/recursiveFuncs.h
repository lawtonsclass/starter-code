#ifndef RECURSIVEFUNCS_H
#define RECURSIVEFUNCS_H

#include <string>

// See the .cpp file for more descriptions of what these functions should do

// returns whether str is a palindrome
bool isPalindrome(const std::string str);

// sums all the digits of a non-negative number n
int sumDigits(int n);

// calculates the length of the C-string s
int stringLength(const char* s);

#endif /* end of include guard: RECURSIVEFUNCS_H */
