#include <iostream>
#include <vector>  // for vector
#include <cstdlib> // for atoi
#include <algorithm>
using namespace std;

int main(int argc, char *argv[]) {
  // the input is guaranteed to have at least 2 elements
  // (see the write-up for an example of the correct formatting)

  vector<int> v;

  // FIXME: read all of the command line arguments into the vector v

  // FIXME: find the min and print it 
  // FIXME: find the max and print it
  // FIXME: find the index of the minimum and print it
  // FIXME: find the index of the maximum and print it
  // FIXME: find & print the second-smallest element (duplicates count as separate numbers!);
  // FIXME: find & print the second-largest element (duplicates count as separate numbers!)

  return 0;
}