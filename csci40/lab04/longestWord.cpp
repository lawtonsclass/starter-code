#include <iostream>
using namespace std;

int main() {
  // FIXME: read words from cin, and keep track of which one is the biggest
  // FIXME: output "The longest word is: ", and then print the longest word (on that same line)

  string currentWord;
  // the following while loop keeps getting a word from cin and putting it into
  // the currentWord variable--it does this until the "end of file" is reached,
  // at which point the expression (cin >> currentWord) will be "falsy" and stop the loop
  while (cin >> currentWord) {
    // FIXME
  }

  return 0;
}